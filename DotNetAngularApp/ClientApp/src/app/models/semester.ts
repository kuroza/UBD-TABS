export interface Semester {
    id: number;
    session: string;
    startDate: string;
    endDate: string;
}

export interface SaveSemester {
    id: number;
    session: string;
    startDate: string;
    endDate: string;
}