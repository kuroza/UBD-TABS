export interface TimeSlot {
    id: number;
    startTime: string;
    endTime: string;
    startAndEndTime: string;
}

export interface SaveTimeSlot {
    id: number;
    startTime: string;
    endTime: string;
}